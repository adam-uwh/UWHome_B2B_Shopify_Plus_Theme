{% comment %} Please don’t update this content {% endcomment %}

{% layout none %}
{
  "product":
    {
      "id":{{ product.id }},
      "metafields":[
        
              {% comment %}
                Variant Metafield
              {% endcomment %}
              {% assign l_variants = product.variants %}
              {% for v_variant in l_variants %}
                {
                  "namespace": {{ v_variant.id }},
                  "key": "uw_sku",
                  "value": {{ v_variant.metafields.custom["uw_sku"] | json }}
                },
              {% endfor %}
        
      ]
    }
}
